name: unitec_vendas_internas
description: App de vendas internas do Unitec ERP (loja — orçamento aberto para o PDV).
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.3.2
  sqflite: ^2.4.1
  path: ^1.9.0
  provider: ^6.1.2
  connectivity_plus: ^6.1.0
  device_info_plus: ^10.1.0
  intl: ^0.19.0
  uuid: ^4.5.1
  wakelock_plus: 1.2.8
  mobile_scanner: ^6.0.2

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
